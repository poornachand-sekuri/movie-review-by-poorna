{
  "name": "movie-review-by-poorna",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "dev": "wrangler types && astro dev",
    "build": "node scripts/cloudflare.mjs production build",
    "check": "wrangler types && astro check",
    "check:guardrails": "node scripts/check-runtime-guardrails.mjs",
    "test:loading": "node --test scripts/check-lounge-loading.mjs",
    "validate": "npm run check:guardrails && npm test && npm run check",
    "preview": "wrangler types && astro preview",
    "cf:types": "wrangler types",
    "deploy:preview": "npm run validate && npm run build:preview && node scripts/cloudflare.mjs preview deploy",
    "build:preview": "node scripts/cloudflare.mjs preview build",
    "test": "node --test scripts/check-lounge-loading.mjs tests/*.test.mjs",
    "deploy:production": "npm run validate && npm run build && node scripts/cloudflare.mjs production deploy",
    "smoke": "node scripts/smoke-site.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "14.2.6",
    "astro": "7.2.10"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3",
    "wrangler": "4.127.1"
  }
}
