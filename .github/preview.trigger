Deploy the current cinema-rebuild branch to the temporary preview Worker and run live D1 API and Lounge page smoke tests.
Deploy the raw unshaded Lounge background with the section stack aligned to the background artwork's central clear lane, preserving visible side landmarks and slightly larger vertical breathing room. Keep the universal focus view for every Lounge content section except the top and bottom navigation banners, and preserve all locked AVIF artwork, internal section geometry, My POV UI, READ MORE artwork and existing card/link actions.