Deploy the current cinema-rebuild branch to the temporary preview Worker and run live D1 API and Lounge page smoke tests.
Protect poster corners so dynamic movie artwork stays inside the decorative red ticket frames without covering the frame artwork.
