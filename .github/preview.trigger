Deploy the current cinema-rebuild branch to the temporary preview Worker and run live D1 API and Lounge page smoke tests.
Deploy the Lounge with the current raw background, centered ambience lane and universal focus view, plus the tighter normal-view content-panel safe inset so no content box intrudes into the background side ambience. Keep top and bottom navigation banners unchanged and preserve all locked AVIF artwork, internal section geometry, My POV UI, READ MORE artwork and existing card/link actions.